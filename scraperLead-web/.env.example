# URL del backend MapLeads (Google Maps scraper)
MAPLEADS_API_URL=http://localhost:8001

# URL del backend InstaLeads (Instagram scraper)
INSTALEADS_API_URL=http://localhost:8002

# Puerto del frontend
PORT=8081

# URL del backend LinkedInLeads (LinkedIn scraper)
LINKEDINLEADS_API_URL=http://localhost:8003

# ── Autenticación ──────────────────────────────────────────────────────────

SESSION_SECRET=daf2c466588d9067fc6cce92b8a103ba6c82ed9919d3c7722f32deeb6ae5638b
SESSION_MAX_AGE=28800

# Contraseña en texto plano (fácil de gestionar):
AUTH_USERS=admin:password123,maria:otrapassword

# Cuando quieras securizarla, reemplaza por el hash:
# python -c "import bcrypt; print(bcrypt.hashpw(b'password123', bcrypt.gensalt()).decode())"
# AUTH_USERS=admin:$2b$12$...,maria:$2b$12$...

# IPs permitidas (vacío = sin restricción en desarrollo)
IP_WHITELIST=